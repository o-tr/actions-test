name: PR Format

on:
  pull_request:
    branches:
      - master
    paths:
      - "**/*.md"

concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.run_id }}
  cancel-in-progress: true

jobs:
  format-check:
    runs-on: arc-runners-ootr-self

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Run Prettier format
        run: npx prettier --write "./**/*.md"

      - name: Commit and Push changes
        env:
          HEAD_REF: ${{ github.head_ref }}
        run: |
          LAST_COMMIT_AUTHOR=$(git log -1 --pretty=format:'%an')
          LAST_COMMIT_EMAIL=$(git log -1 --pretty=format:'%ae')
          git config --local user.name "${LAST_COMMIT_AUTHOR}"
          git config --local user.email "${LAST_COMMIT_EMAIL}"
          git add .
          if git commit -m "[GitHub Actions] swift-format"; then
            echo "Changes committed, pushing to repository..."
            git push origin HEAD:"${HEAD_REF}"
          else
            echo "No changes to commit"
          fi
